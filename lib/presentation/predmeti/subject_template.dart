import 'package:flutter/material.dart';

class SubjectTemplate extends StatelessWidget {
  const SubjectTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
